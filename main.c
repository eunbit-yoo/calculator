/**
 * author : eunbit-yoo
 **/
#include <stdio.h>

int add(int, int);
int sub(int,int);
int main(int grgc, char* argv[]) {
	int a, b, c, d;
	char op;
	int d;
	printf("Please");
	scanf("%d %c %d", &a, &op, &b);
	switch(op) {
	};
}